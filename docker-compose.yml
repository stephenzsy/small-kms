services:

  krakend:
    build:
      context: ./krakend      
    ports:
      - "8080:8080"
    links: 
      - backend:backend

  backend:
    build:
      context: ./backend
      dockerfile: dev.dockerfile
    volumes:
      - ./backend:/usr/src/app

      # frontend:
      #   build:
      #     context: ./frontend
      #     dockerfile: dev.dockerfile
      #   volumes:
      #     - ./frontend:/app

  radius:
    build: radius/docker
    volumes:
      - ./radius/log:/opt/var/log/radius
    links:
      - krakend:krakend
    #tty: true
    #stdin_open: true
    #command: /bin/sh

